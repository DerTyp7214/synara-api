# Project Instructions

## Dependency Updates
To check for dependency updates, run the following Gradle task:
```sh
./gradlew checkUpdates
```
Then read `updates.txt` and apply the changes to `gradle/libs.versions.toml`.
